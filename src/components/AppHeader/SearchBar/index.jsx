export default function SearchBar(props){

    // const dispatch = useDispatch()

    // useEffect(() => {

    // })

    return (
        <>
            <h1>this is search bar</h1>
        </>
    )
}